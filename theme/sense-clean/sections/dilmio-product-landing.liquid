{{ 'dilmio-product.css' | asset_url | stylesheet_tag }}

{%- liquid
  assign landing_product = nil
  if section.settings.product != blank
    assign landing_product = section.settings.product
  elsif request.page_type == 'product'
    assign landing_product = product
  endif
-%}
{%- if request.page_type == 'product' -%}
  {%- assign hero_headline        = landing_product.metafields.custom.dilmio_hero_headline.value -%}
  {%- assign hero_subheadline     = landing_product.metafields.custom.dilmio_hero_subheadline.value -%}
  {%- assign demo_video_url       = landing_product.metafields.custom.dilmio_demo_video_url.value -%}
  {%- assign benefit_1            = landing_product.metafields.custom.dilmio_benefit_1.value -%}
  {%- assign benefit_2            = landing_product.metafields.custom.dilmio_benefit_2.value -%}
  {%- assign benefit_3            = landing_product.metafields.custom.dilmio_benefit_3.value -%}
  {%- assign benefit_4            = landing_product.metafields.custom.custom_dilmio_benefit_4.value -%}
  {%- assign faq_question_1       = landing_product.metafields.custom.dilmio_faq_question_1.value -%}
  {%- assign faq_question_2       = landing_product.metafields.custom.custom_dilmio_faq_question_2.value -%}
  {%- assign faq_answer_2         = landing_product.metafields.custom.custom_dilmio_faq_answer_2.value -%}
  {%- assign faq_question_3       = landing_product.metafields.custom.custom_dilmio_faq_question_3.value -%}
  {%- assign faq_answer_3         = landing_product.metafields.custom.custom_dilmio_faq_answer_3.value -%}
  {%- assign how_it_works_heading = landing_product.metafields.custom.dilmio_how_it_works_heading.value -%}
  {%- assign how_step_1_title     = landing_product.metafields.custom.dilmio_how_step_1_title.value -%}
  {%- assign how_step_1_text      = landing_product.metafields.custom.dilmio_how_step_1_text.value -%}
  {%- assign how_step_2_title     = landing_product.metafields.custom.dilmio_how_step_2_title.value -%}
  {%- assign how_step_2_text      = landing_product.metafields.custom.dilmio_how_step_2_text.value -%}
  {%- assign how_step_3_title     = landing_product.metafields.custom.dilmio_how_step_3_title.value -%}
  {%- assign how_step_3_text      = landing_product.metafields.custom.dilmio_how_step_3_text.value -%}
{%- else -%}
  {%- assign hero_headline        = section.settings.hero_headline -%}
  {%- assign hero_subheadline     = section.settings.hero_subheadline -%}
  {%- assign demo_video_url       = section.settings.demo_video_url -%}
  {%- assign benefit_1            = section.settings.benefit_1 -%}
  {%- assign benefit_2            = section.settings.benefit_2 -%}
  {%- assign benefit_3            = section.settings.benefit_3 -%}
  {%- assign benefit_4            = section.settings.benefit_4 -%}
  {%- assign faq_question_1       = section.settings.faq_question_1 -%}
  {%- assign faq_question_2       = section.settings.faq_question_2 -%}
  {%- assign faq_answer_2         = section.settings.faq_answer_2 -%}
  {%- assign faq_question_3       = section.settings.faq_question_3 -%}
  {%- assign faq_answer_3         = section.settings.faq_answer_3 -%}
  {%- assign how_it_works_heading = section.settings.how_it_works_heading -%}
  {%- assign how_step_1_title     = section.settings.how_it_works_step_1_title -%}
  {%- assign how_step_1_text      = section.settings.how_it_works_step_1_text -%}
  {%- assign how_step_2_title     = section.settings.how_it_works_step_2_title -%}
  {%- assign how_step_2_text      = section.settings.how_it_works_step_2_text -%}
  {%- assign how_step_3_title     = section.settings.how_it_works_step_3_title -%}
  {%- assign how_step_3_text      = section.settings.how_it_works_step_3_text -%}
{%- endif -%}

<section
  id="dilmio-landing-{{ section.id }}"
  class="dilmio-landing"
  data-section-id="{{ section.id }}"
>

  {%- if landing_product == blank -%}

    {%- if request.design_mode -%}
      <div class="dilmio-placeholder">
        <p>Select a product in the Shopify customizer to activate this section.</p>
      </div>
    {%- endif -%}

  {%- else -%}

    {%- assign current_variant = landing_product.selected_or_first_available_variant -%}

    <script id="dilmio-variants-{{ section.id }}" type="application/json">
      [
        {%- for variant in landing_product.variants -%}
          {
            "id": {{ variant.id }},
            "title": {{ variant.title | json }},
            "price_formatted": {{ variant.price | money | json }},
            "available": {{ variant.available }},
            "option1": {{ variant.option1 | json }},
            "option2": {{ variant.option2 | json }},
            "option3": {{ variant.option3 | json }}
          }{% unless forloop.last %},{% endunless %}
        {%- endfor -%}
      ]
    </script>

    <div class="dilmio-hero">
      {%- assign resolved_headline = hero_headline | default: landing_product.title -%}
      <div class="dilmio-hero__headline">{{ resolved_headline | newline_to_br }}</div>
      {%- if hero_subheadline != blank -%}
        <p class="dilmio-hero__subheadline">{{ hero_subheadline }}</p>
      {%- endif -%}
    </div>

    <div id="dilmio-buy-core" class="dilmio-landing__inner">

      <div class="dilmio-landing__media">
        {%- assign first_img = landing_product.images.first -%}
        {%- if first_img -%}
          <div class="dilmio-gallery">
            <div class="dilmio-gallery__main-wrap">
              <img
                id="dilmio-gallery-main-{{ section.id }}"
                src="{{ first_img | image_url: width: 800 }}"
                alt="{{ first_img.alt | escape }}"
                width="800"
                loading="eager"
              >
            </div>
            {%- if landing_product.images.size > 1 -%}
              <div class="dilmio-gallery__thumbs">
                {%- for img in landing_product.images -%}
                  <button
                    type="button"
                    class="dilmio-gallery__thumb{% if forloop.first %} is-active{% endif %}"
                    data-src="{{ img | image_url: width: 800 }}"
                    data-alt="{{ img.alt | escape }}"
                  >
                    <img
                      src="{{ img | image_url: width: 150 }}"
                      alt="{{ img.alt | escape }}"
                      width="150"
                      loading="lazy"
                    >
                  </button>
                {%- endfor -%}
              </div>
              <script>
                (function(){
                  var mainImg = document.getElementById('dilmio-gallery-main-{{ section.id }}');
                  if (!mainImg) return;
                  document.querySelectorAll('#dilmio-landing-{{ section.id }} .dilmio-gallery__thumb').forEach(function(btn){
                    btn.addEventListener('click', function(){
                      mainImg.src = btn.dataset.src;
                      mainImg.alt = btn.dataset.alt || '';
                      document.querySelectorAll('#dilmio-landing-{{ section.id }} .dilmio-gallery__thumb').forEach(function(b){
                        b.classList.remove('is-active');
                      });
                      btn.classList.add('is-active');
                    });
                  });
                })();
              </script>
            {%- endif -%}
          </div>
        {%- endif -%}
      </div>

      <div class="dilmio-landing__form">

        <h1 class="dilmio-landing__title">{{ landing_product.title }}</h1>

        {%- assign rating_avg   = landing_product.metafields.custom.dilmio_rating_avg.value -%}
        {%- assign rating_count = landing_product.metafields.custom.dilmio_rating_count.value -%}
        {%- if rating_avg != blank and rating_count != blank -%}
          {%- assign star_pct = rating_avg | divided_by: 5.0 | times: 100 | round -%}
          <div class="dilmio-rating">
            <span class="dilmio-rating__stars" aria-label="{{ rating_avg }} de 5 estrellas">
              <span class="dilmio-rating__stars-bg">★★★★★</span>
              <span class="dilmio-rating__stars-fg" style="width:{{ star_pct }}%">★★★★★</span>
            </span>
            <span class="dilmio-rating__avg">{{ rating_avg }}</span>
            <span class="dilmio-rating__count">({{ rating_count }})</span>
          </div>
        {%- endif -%}

        <div class="dilmio-landing__price" id="dilmio-price-{{ section.id }}">
          {{ current_variant.price | money }}
        </div>

        {%- unless landing_product.has_only_default_variant -%}
          <div class="dilmio-options" id="dilmio-options-{{ section.id }}">
            {%- for option in landing_product.options_with_values -%}
              {%- assign opt_index = forloop.index0 -%}
              <div class="dilmio-option-group" data-option-index="{{ opt_index }}">
                <p class="dilmio-option-group__label">{{ option.name }}</p>
                <div class="dilmio-option-group__values">
                  {%- for value in option.values -%}
                    <button
                      type="button"
                      class="dilmio-option-btn{% if option.selected_value == value %} is-selected{% endif %}"
                      data-option-index="{{ opt_index }}"
                      data-value="{{ value | escape }}"
                    >
                      {{ value }}
                    </button>
                  {%- endfor -%}
                </div>
              </div>
            {%- endfor -%}
          </div>
        {%- endunless -%}

        {%- if section.settings.show_pack -%}
          {%- assign has_packs = false -%}
          {%- if section.settings.pack_label_1 != blank and section.settings.pack_quantity_1 > 0 -%}{%- assign has_packs = true -%}{%- endif -%}
          {%- if section.settings.pack_label_2 != blank and section.settings.pack_quantity_2 > 0 -%}{%- assign has_packs = true -%}{%- endif -%}
          {%- if section.settings.pack_label_3 != blank and section.settings.pack_quantity_3 > 0 -%}{%- assign has_packs = true -%}{%- endif -%}
          {%- if has_packs -%}
          {%- assign first_pack_rendered = false -%}
          <div class="dilmio-packs" id="dilmio-packs-{{ section.id }}">
            <p class="dilmio-packs__label">{{ section.settings.pack_section_label }}</p>
            <div class="dilmio-packs__values">

              {%- if section.settings.pack_label_1 != blank and section.settings.pack_quantity_1 > 0 -%}
                <button
                  type="button"
                  class="dilmio-pack-btn{%- unless first_pack_rendered %} is-selected{%- endunless -%}"
                  data-quantity="{{ section.settings.pack_quantity_1 }}"
                >{{ section.settings.pack_label_1 }}</button>
                {%- assign first_pack_rendered = true -%}
              {%- endif -%}

              {%- if section.settings.pack_label_2 != blank and section.settings.pack_quantity_2 > 0 -%}
                <button
                  type="button"
                  class="dilmio-pack-btn{%- unless first_pack_rendered %} is-selected{%- endunless -%}"
                  data-quantity="{{ section.settings.pack_quantity_2 }}"
                >{{ section.settings.pack_label_2 }}</button>
                {%- assign first_pack_rendered = true -%}
              {%- endif -%}

              {%- if section.settings.pack_label_3 != blank and section.settings.pack_quantity_3 > 0 -%}
                <button
                  type="button"
                  class="dilmio-pack-btn{%- unless first_pack_rendered %} is-selected{%- endunless -%}"
                  data-quantity="{{ section.settings.pack_quantity_3 }}"
                >{{ section.settings.pack_label_3 }}</button>
              {%- endif -%}

            </div>
          </div>
          {%- endif -%}
        {%- endif -%}

        <div class="dilmio-cta-wrap">
          <button
            type="button"
            id="dilmio-cta-{{ section.id }}"
            class="dilmio-cta"
            data-variant-id="{{ current_variant.id }}"
            data-add-label="{{ section.settings.cta_add_to_cart_label | escape }}"
            data-sold-out-label="{{ section.settings.cta_sold_out_label | escape }}"
            data-unavailable-label="No disponible"
            {% unless current_variant.available %}disabled{% endunless %}
          >
            {%- if current_variant.available -%}{{ section.settings.cta_add_to_cart_label }}{%- else -%}{{ section.settings.cta_sold_out_label }}{%- endif -%}
          </button>
          <div
            id="dilmio-error-{{ section.id }}"
            class="dilmio-error"
            role="alert"
            style="display:none;"
          ></div>
        </div>

        {%- if section.settings.trust_1 != blank or section.settings.trust_2 != blank or section.settings.trust_3 != blank -%}
          <div class="dilmio-trust-inline">
            {%- if section.settings.trust_1 != blank -%}
              <span class="dilmio-trust-inline__item">
                {%- if section.settings.trust_icon_1 != blank and section.settings.trust_icon_1 != 'none' -%}
                  <span class="dilmio-trust__icon">{% render 'dilmio-trust-icon', icon: section.settings.trust_icon_1 %}</span>
                {%- endif -%}
                {{ section.settings.trust_1 }}
              </span>
            {%- endif -%}
            {%- if section.settings.trust_2 != blank -%}
              <span class="dilmio-trust-inline__item">
                {%- if section.settings.trust_icon_2 != blank and section.settings.trust_icon_2 != 'none' -%}
                  <span class="dilmio-trust__icon">{% render 'dilmio-trust-icon', icon: section.settings.trust_icon_2 %}</span>
                {%- endif -%}
                {{ section.settings.trust_2 }}
              </span>
            {%- endif -%}
            {%- if section.settings.trust_3 != blank -%}
              <span class="dilmio-trust-inline__item">
                {%- if section.settings.trust_icon_3 != blank and section.settings.trust_icon_3 != 'none' -%}
                  <span class="dilmio-trust__icon">{% render 'dilmio-trust-icon', icon: section.settings.trust_icon_3 %}</span>
                {%- endif -%}
                {{ section.settings.trust_3 }}
              </span>
            {%- endif -%}
          </div>
        {%- endif -%}

      </div>
    </div>

    {%- if section.settings.show_how_it_works -%}
      {%- assign has_steps = false -%}
      {%- if how_step_1_title != blank or how_step_1_text != blank -%}
        {%- assign has_steps = true -%}
      {%- endif -%}
      {%- if how_step_2_title != blank or how_step_2_text != blank -%}
        {%- assign has_steps = true -%}
      {%- endif -%}
      {%- if how_step_3_title != blank or how_step_3_text != blank -%}
        {%- assign has_steps = true -%}
      {%- endif -%}
      {%- if has_steps -%}
        <div class="dilmio-steps">
          {%- if how_it_works_heading != blank -%}
            <h2 class="dilmio-steps__heading">{{ how_it_works_heading }}</h2>
          {%- endif -%}
          <ol class="dilmio-steps__list">
            {%- if how_step_1_title != blank or how_step_1_text != blank -%}
              <li class="dilmio-steps__item">
                <div class="dilmio-steps__body">
                  {%- if how_step_1_title != blank -%}
                    <span class="dilmio-steps__title">{{ how_step_1_title }}</span>
                  {%- endif -%}
                  {%- if how_step_1_text != blank -%}
                    <span class="dilmio-steps__text">{{ how_step_1_text }}</span>
                  {%- endif -%}
                </div>
              </li>
            {%- endif -%}
            {%- if how_step_2_title != blank or how_step_2_text != blank -%}
              <li class="dilmio-steps__item">
                <div class="dilmio-steps__body">
                  {%- if how_step_2_title != blank -%}
                    <span class="dilmio-steps__title">{{ how_step_2_title }}</span>
                  {%- endif -%}
                  {%- if how_step_2_text != blank -%}
                    <span class="dilmio-steps__text">{{ how_step_2_text }}</span>
                  {%- endif -%}
                </div>
              </li>
            {%- endif -%}
            {%- if how_step_3_title != blank or how_step_3_text != blank -%}
              <li class="dilmio-steps__item">
                <div class="dilmio-steps__body">
                  {%- if how_step_3_title != blank -%}
                    <span class="dilmio-steps__title">{{ how_step_3_title }}</span>
                  {%- endif -%}
                  {%- if how_step_3_text != blank -%}
                    <span class="dilmio-steps__text">{{ how_step_3_text }}</span>
                  {%- endif -%}
                </div>
              </li>
            {%- endif -%}
          </ol>
        </div>
      {%- endif -%}
    {%- endif -%}

    {%- assign show_video = false -%}
    {%- if request.page_type == 'product' and demo_video_url != blank -%}
      {%- assign show_video = true -%}
    {%- elsif section.settings.show_demo_video and demo_video_url != blank -%}
      {%- assign show_video = true -%}
    {%- endif -%}

    {%- assign has_benefits = false -%}
    {%- if benefit_1 != blank or benefit_2 != blank or benefit_3 != blank or benefit_4 != blank -%}
      {%- assign has_benefits = true -%}
    {%- endif -%}

    {%- if show_video -%}
      <div class="dilmio-demo-section">
        {%- if section.settings.demo_video_heading != blank -%}
          <div class="dilmio-demo-header">
            <h2 class="dilmio-demo-header__title">{{ section.settings.demo_video_heading }}</h2>
          </div>
        {%- endif -%}
        <div class="dilmio-demo-split">
          <div class="dilmio-demo-split__video">
            <video
              class="dilmio-demo-split__player"
              src="{{ demo_video_url | escape }}"
              autoplay
              muted
              loop
              playsinline
            ></video>
          </div>
          {%- if has_benefits -%}
            <div class="dilmio-demo-split__content">
              {%- if section.settings.benefits_title != blank -%}
                <h3 class="dilmio-demo-split__benefits-title">{{ section.settings.benefits_title }}</h3>
              {%- endif -%}
              <ul class="dilmio-demo-split__benefits">
                {%- if benefit_1 != blank -%}
                  <li class="dilmio-demo-split__benefit">{{ benefit_1 }}</li>
                {%- endif -%}
                {%- if benefit_2 != blank -%}
                  <li class="dilmio-demo-split__benefit">{{ benefit_2 }}</li>
                {%- endif -%}
                {%- if benefit_3 != blank -%}
                  <li class="dilmio-demo-split__benefit">{{ benefit_3 }}</li>
                {%- endif -%}
                {%- if benefit_4 != blank -%}
                  <li class="dilmio-demo-split__benefit">{{ benefit_4 }}</li>
                {%- endif -%}
              </ul>
            </div>
          {%- endif -%}
        </div>
      </div>
    {%- elsif section.settings.show_benefits and has_benefits -%}
      <div class="dilmio-benefits">
        {%- if section.settings.benefits_title != blank -%}
          <h2 class="dilmio-benefits__title">{{ section.settings.benefits_title }}</h2>
        {%- endif -%}
        <ul class="dilmio-benefits__list">
          {%- if benefit_1 != blank -%}
            <li class="dilmio-benefits__item">{{ benefit_1 }}</li>
          {%- endif -%}
          {%- if benefit_2 != blank -%}
            <li class="dilmio-benefits__item">{{ benefit_2 }}</li>
          {%- endif -%}
          {%- if benefit_3 != blank -%}
            <li class="dilmio-benefits__item">{{ benefit_3 }}</li>
          {%- endif -%}
          {%- if benefit_4 != blank -%}
            <li class="dilmio-benefits__item">{{ benefit_4 }}</li>
          {%- endif -%}
        </ul>
      </div>
    {%- endif -%}

    {%- if section.settings.show_trust -%}
      {%- if section.settings.trust_1 != blank or section.settings.trust_2 != blank or section.settings.trust_3 != blank -%}
        <div class="dilmio-trust">
          <ul class="dilmio-trust__list">
            {%- if section.settings.trust_1 != blank -%}
              <li class="dilmio-trust__item">
                {%- if section.settings.trust_icon_1 != blank and section.settings.trust_icon_1 != 'none' -%}
                  <span class="dilmio-trust__icon">{% render 'dilmio-trust-icon', icon: section.settings.trust_icon_1 %}</span>
                {%- endif -%}
                {{ section.settings.trust_1 }}
              </li>
            {%- endif -%}
            {%- if section.settings.trust_2 != blank -%}
              <li class="dilmio-trust__item">
                {%- if section.settings.trust_icon_2 != blank and section.settings.trust_icon_2 != 'none' -%}
                  <span class="dilmio-trust__icon">{% render 'dilmio-trust-icon', icon: section.settings.trust_icon_2 %}</span>
                {%- endif -%}
                {{ section.settings.trust_2 }}
              </li>
            {%- endif -%}
            {%- if section.settings.trust_3 != blank -%}
              <li class="dilmio-trust__item">
                {%- if section.settings.trust_icon_3 != blank and section.settings.trust_icon_3 != 'none' -%}
                  <span class="dilmio-trust__icon">{% render 'dilmio-trust-icon', icon: section.settings.trust_icon_3 %}</span>
                {%- endif -%}
                {{ section.settings.trust_3 }}
              </li>
            {%- endif -%}
          </ul>
        </div>
      {%- endif -%}
    {%- endif -%}

    {%- if section.settings.show_faq -%}
      {%- if faq_question_1 != blank or faq_question_2 != blank or faq_question_3 != blank -%}
        <div class="dilmio-faq">
          {%- if section.settings.faq_title != blank -%}
            <h2 class="dilmio-faq__title">{{ section.settings.faq_title }}</h2>
          {%- endif -%}
          {%- if faq_question_1 != blank -%}
            <details class="dilmio-faq__item">
              <summary class="dilmio-faq__question">{{ faq_question_1 }}</summary>
              {%- if request.page_type == 'product' -%}
                {%- if landing_product.metafields.custom.dilmio_faq_answer_1.value != blank -%}
                  <div class="dilmio-faq__answer">{{ landing_product.metafields.custom.dilmio_faq_answer_1.value | newline_to_br }}</div>
                {%- endif -%}
              {%- elsif section.settings.faq_answer_1 != blank -%}
                <div class="dilmio-faq__answer">{{ section.settings.faq_answer_1 }}</div>
              {%- endif -%}
            </details>
          {%- endif -%}
          {%- if faq_question_2 != blank -%}
            <details class="dilmio-faq__item">
              <summary class="dilmio-faq__question">{{ faq_question_2 }}</summary>
              {%- if faq_answer_2 != blank -%}
                <div class="dilmio-faq__answer">{{ faq_answer_2 | newline_to_br }}</div>
              {%- endif -%}
            </details>
          {%- endif -%}
          {%- if faq_question_3 != blank -%}
            <details class="dilmio-faq__item">
              <summary class="dilmio-faq__question">{{ faq_question_3 }}</summary>
              {%- if faq_answer_3 != blank -%}
                <div class="dilmio-faq__answer">{{ faq_answer_3 | newline_to_br }}</div>
              {%- endif -%}
            </details>
          {%- endif -%}
        </div>
      {%- endif -%}
    {%- endif -%}

    <div
      id="dilmio-sticky-{{ section.id }}"
      class="dilmio-sticky"
      aria-hidden="true"
    >
      <div class="dilmio-sticky__inner">
        <div class="dilmio-sticky__info">
          <span class="dilmio-sticky__title">{{ landing_product.title }}</span>
          {%- unless landing_product.has_only_default_variant -%}
            <span
              class="dilmio-sticky__variant"
              id="dilmio-sticky-variant-{{ section.id }}"
            >{{ current_variant.title }}</span>
          {%- endunless -%}
          <span
            class="dilmio-sticky__price"
            id="dilmio-sticky-price-{{ section.id }}"
          >{{ current_variant.price | money }}</span>
        </div>
        <button
          type="button"
          id="dilmio-sticky-cta-{{ section.id }}"
          class="dilmio-cta dilmio-sticky__cta"
          data-variant-id="{{ current_variant.id }}"
          data-add-label="{{ section.settings.cta_add_to_cart_label | escape }}"
          data-sold-out-label="{{ section.settings.cta_sold_out_label | escape }}"
          data-unavailable-label="No disponible"
          {% unless current_variant.available %}disabled{% endunless %}
        >
          {%- if current_variant.available -%}{{ section.settings.cta_add_to_cart_label }}{%- else -%}{{ section.settings.cta_sold_out_label }}{%- endif -%}
        </button>
      </div>
    </div>

  {%- endif -%}

</section>

<script src="{{ 'dilmio-product.js' | asset_url }}" defer="defer"></script>

{% schema %}
{
  "name": "DILMIO Product Landing",
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "Product"
    },
    {
      "type": "header",
      "content": "Pack selector"
    },
    {
      "type": "checkbox",
      "id": "show_pack",
      "label": "Show pack selector",
      "default": false
    },
    {
      "type": "text",
      "id": "pack_label_1",
      "label": "Pack 1 — label",
      "placeholder": "e.g. 1 unit"
    },
    {
      "type": "number",
      "id": "pack_quantity_1",
      "label": "Pack 1 — quantity",
      "default": 0
    },
    {
      "type": "text",
      "id": "pack_label_2",
      "label": "Pack 2 — label",
      "placeholder": "e.g. 2 units"
    },
    {
      "type": "number",
      "id": "pack_quantity_2",
      "label": "Pack 2 — quantity",
      "default": 0
    },
    {
      "type": "text",
      "id": "pack_label_3",
      "label": "Pack 3 — label",
      "placeholder": "e.g. 3 units"
    },
    {
      "type": "number",
      "id": "pack_quantity_3",
      "label": "Pack 3 — quantity",
      "default": 0
    },
    {
      "type": "header",
      "content": "Benefits"
    },
    {
      "type": "checkbox",
      "id": "show_benefits",
      "label": "Show benefits block",
      "default": false
    },
    {
      "type": "text",
      "id": "benefits_title",
      "label": "Benefits title",
      "placeholder": "e.g. Why customers love it"
    },
    {
      "type": "header",
      "content": "Trust"
    },
    {
      "type": "checkbox",
      "id": "show_trust",
      "label": "Show trust bullets",
      "default": false
    },
    {
      "type": "text",
      "id": "trust_1",
      "label": "Trust bullet 1",
      "placeholder": "e.g. Free shipping on orders over €30"
    },
    {
      "type": "text",
      "id": "trust_2",
      "label": "Trust bullet 2",
      "placeholder": "e.g. 30-day returns"
    },
    {
      "type": "text",
      "id": "trust_3",
      "label": "Trust bullet 3",
      "placeholder": "e.g. 100% satisfaction guarantee"
    },
    {
      "type": "select",
      "id": "trust_icon_1",
      "label": "Trust icon 1",
      "default": "none",
      "options": [
        {"value": "none", "label": "None"},
        {"value": "check", "label": "Check"},
        {"value": "shield", "label": "Shield"},
        {"value": "lock", "label": "Lock"},
        {"value": "star", "label": "Star"},
        {"value": "heart", "label": "Heart"},
        {"value": "truck", "label": "Truck"},
        {"value": "box", "label": "Box"},
        {"value": "return", "label": "Return"},
        {"value": "headset", "label": "Headset"},
        {"value": "leaf", "label": "Leaf"},
        {"value": "globe", "label": "Globe"},
        {"value": "award", "label": "Award"},
        {"value": "lightning", "label": "Lightning"},
        {"value": "clock", "label": "Clock"},
        {"value": "phone", "label": "Phone"},
        {"value": "users", "label": "Users"},
        {"value": "chart", "label": "Chart"},
        {"value": "download", "label": "Download"},
        {"value": "key", "label": "Key"}
      ]
    },
    {
      "type": "select",
      "id": "trust_icon_2",
      "label": "Trust icon 2",
      "default": "none",
      "options": [
        {"value": "none", "label": "None"},
        {"value": "check", "label": "Check"},
        {"value": "shield", "label": "Shield"},
        {"value": "lock", "label": "Lock"},
        {"value": "star", "label": "Star"},
        {"value": "heart", "label": "Heart"},
        {"value": "truck", "label": "Truck"},
        {"value": "box", "label": "Box"},
        {"value": "return", "label": "Return"},
        {"value": "headset", "label": "Headset"},
        {"value": "leaf", "label": "Leaf"},
        {"value": "globe", "label": "Globe"},
        {"value": "award", "label": "Award"},
        {"value": "lightning", "label": "Lightning"},
        {"value": "clock", "label": "Clock"},
        {"value": "phone", "label": "Phone"},
        {"value": "users", "label": "Users"},
        {"value": "chart", "label": "Chart"},
        {"value": "download", "label": "Download"},
        {"value": "key", "label": "Key"}
      ]
    },
    {
      "type": "select",
      "id": "trust_icon_3",
      "label": "Trust icon 3",
      "default": "none",
      "options": [
        {"value": "none", "label": "None"},
        {"value": "check", "label": "Check"},
        {"value": "shield", "label": "Shield"},
        {"value": "lock", "label": "Lock"},
        {"value": "star", "label": "Star"},
        {"value": "heart", "label": "Heart"},
        {"value": "truck", "label": "Truck"},
        {"value": "box", "label": "Box"},
        {"value": "return", "label": "Return"},
        {"value": "headset", "label": "Headset"},
        {"value": "leaf", "label": "Leaf"},
        {"value": "globe", "label": "Globe"},
        {"value": "award", "label": "Award"},
        {"value": "lightning", "label": "Lightning"},
        {"value": "clock", "label": "Clock"},
        {"value": "phone", "label": "Phone"},
        {"value": "users", "label": "Users"},
        {"value": "chart", "label": "Chart"},
        {"value": "download", "label": "Download"},
        {"value": "key", "label": "Key"}
      ]
    },
    {
      "type": "header",
      "content": "FAQ"
    },
    {
      "type": "checkbox",
      "id": "show_faq",
      "label": "Show FAQ block",
      "default": false
    },
    {
      "type": "text",
      "id": "faq_title",
      "label": "FAQ title",
      "placeholder": "e.g. Frequently Asked Questions"
    },
    {
      "type": "header",
      "content": "Demo video"
    },
    {
      "type": "checkbox",
      "id": "show_demo_video",
      "label": "Show demo video",
      "default": false
    },
    {
      "type": "text",
      "id": "demo_video_heading",
      "label": "Video heading",
      "placeholder": "e.g. See it in action"
    },
    {
      "type": "header",
      "content": "How it works"
    },
    {
      "type": "checkbox",
      "id": "show_how_it_works",
      "label": "Show how it works",
      "default": false
    },
    {
      "type": "header",
      "content": "Labels"
    },
    {
      "type": "text",
      "id": "cta_add_to_cart_label",
      "label": "CTA — Add to cart",
      "default": "Añadir al carrito"
    },
    {
      "type": "text",
      "id": "cta_sold_out_label",
      "label": "CTA — Sold out",
      "default": "Agotado"
    },
    {
      "type": "select",
      "id": "pack_section_label",
      "label": "Pack section label",
      "default": "Pack",
      "options": [
        {"value": "Pack", "label": "Pack"},
        {"value": "Plan", "label": "Plan"},
        {"value": "Licencia", "label": "Licencia"},
        {"value": "Sesiones", "label": "Sesiones"},
        {"value": "Cantidad", "label": "Cantidad"}
      ]
    }
  ],
  "presets": [
    {
      "name": "DILMIO Product Landing"
    }
  ]
}
{% endschema %}
